import { ReferalApplication } from './referal-application.entity';

describe('ReferalApplication.Entity', () => {
  it('should create an instance', () => {
    expect(new ReferalApplication()).toBeTruthy();
  });
});
